# Design system

This file is a stub. The canonical design rules now live in
[`../../docs/design.md`](../../docs/design.md).

`tokens.css` and `utilities.css` in this directory are still the source
files. `docs/design.md` is what tells you _how_ to use them, what's
forbidden, and which lint catches violations.

If you're an agent: read [`../../AGENTS.md`](../../AGENTS.md) first.
